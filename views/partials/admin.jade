- if(admin == true)
    .admintools.article.panel.panel-default.panel-content(ng-init='init()')
        h1.content-title Admin Tools
        hr
        h2 Hits: {{ hits }}
        h2 Status:&nbsp;
            button.btn(ng-click='toggleStatus()') {{ status }}
